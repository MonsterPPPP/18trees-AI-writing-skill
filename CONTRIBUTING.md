# 贡献指南

## 最重要的一条：这是累计式 skill

**只增量优化，不回退。**

- 新版本只能在上一版本基础上**增加、修正或细化**规则。
- **不得因为新增规则而删除已经验证有效的旧规则。**
- 除非维护者明确要求废弃某条规则，否则旧规则持续有效。
- 新规则与旧规则冲突时：保留更高优先级的明确要求 → 对冲突点做局部修订 → **不得整份重写导致旧约束丢失**。
- 更新时尽量用 patch / append / refine，不要 rewrite from scratch。

如果你觉得某条旧规则有问题，正确做法是**提出一条新规则来细化或限定它**，并说明适用边界，而不是删掉它。

## 已确认不可回退的规则

以下 10 条被维护者确认有效，任何 PR 都不得弱化：

1. 默认写完整段落，不默认写短句。
2. 独立短句只作为少量强调手段，不作为常规节奏。
3. 允许少量加粗，但必须克制。
4. 章节标题使用统一编号体系。
5. 列表、表格、分条由内容决定，不机械使用。
6. 严控"不是……而是……"等高 AI 味句式。
7. 观点保真优先于稳健化。
8. 事实可以纠正，作者判断不自动中和。
9. 优先保留作者独特、锋利、略带冒犯或极端的个人表达。
10. 编辑的任务是"整理原文"，不是重新写一篇更标准的文章。

---

## 规则只维护在一个地方

`dist/voice-preserving-essay-editor.md` 是**生成物**，不要直接编辑它。

改规则的流程：

1. 改 `skills/voice-preserving-essay-editor/` 下的文件（`SKILL.md` 或 `references/`）
2. 运行生成脚本：

```bash
bash scripts/build-dist.sh
```

3. 把 `dist/` 的改动一起提交

`dist/` 是给没有 skill 机制的工具（网页版 ChatGPT、DeepSeek、Kimi 等）用的——用户直接复制粘贴整份文件，所以它必须包含**全部**约束，不能有任何外部引用。

---

## 改哪一层？

| 你想加的规则 | 放哪 |
|---|---|
| 任务本质、编辑优先级、跨所有场景的硬约束 | `SKILL.md` |
| 段落长度、节奏、拆合段落 | `references/paragraph-rhythm.md` |
| 标题、加粗、列表、表格、Markdown 排版 | `references/formatting.md` |
| 什么能改什么不能改、事实核验、引文 | `references/editing-boundaries.md` |
| AI 味词汇和句式黑名单 | `references/ai-tells.md` |
| 编辑权限、编辑强度、作者声音 | `references/core-principles.md` |
| 执行流程、输出前自检 | `references/workflow-checklist.md` |

**不要把细则塞进 `SKILL.md`。** 入口文件保持精简，细则放 `references/` 按需加载——这是让 skill 在 Claude Code 和 Codex 里都跑得好的前提。

---

## 提交示例

`examples/` 的价值高于规则本身——一对真实的"原稿 → 成稿"比十条抽象规则更能说明问题。

提交示例时：

1. 用**真实的**语音转写稿（脱敏即可），不要手写一份"假装很乱"的稿子
2. 附上逐条对照说明：每处改动对应哪条规则
3. 如果整理过程中发现了规则的漏洞或冲突，在 PR 里指出来——这比示例本身更有价值

## 提交 PR

- 一个 PR 只做一件事：加一条规则，或加一个示例，或修一个错误
- PR 描述里写清楚：**改了什么，为什么，对应哪条已确认的规则没有被破坏**
- 涉及规则改动的，说明它和现有的 10 条不可回退规则是否冲突

## 内容边界

这个 skill 处理**作者自己的稿子**。不接受：

- 用于批量洗稿、伪原创他人内容的用法
- 会诱导 AI 冒充特定真人身份的功能
- 把他人作品改头换面据为己有的工作流

## Commit 信息

中文或英文都可以，说清楚改了什么。建议格式：

```
规则：增加 XXX 的例外情况
示例：新增一段口播稿整理对照
修复：修正 dist/ 与 skills/ 不一致
```
